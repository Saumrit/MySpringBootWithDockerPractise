spring:
#  profiles.active: local
  data:
    mongodb:
      uri: mongodb+srv://admin:admin@cluster0.azoiabn.mongodb.net/mySpringBootPractise?appName=Cluster0&retryWrites=true&w=majority

management:
  endpoints:
    web:
      exposure:
        include: mappings
springdoc:
  swagger-ui:
    path: "/swagger-ui.html"